<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    android:background="#ABDEE1"
    android:gravity="center_vertical">
    <androidx.recyclerview.widget.RecyclerView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/rvcartas">
    </androidx.recyclerview.widget.RecyclerView>
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="#ABDEE1"
        android:fontFamily="monospace"
        android:textSize="20dp"
        android:id="@+id/puntuacion"
        android:layout_below="@id/rvcartas"/>
    <Button
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:text="SOLICITAR NÚMERO"
        android:textColor="#FFF"
        android:fontFamily="monospace"
        android:textSize="20dp"
        android:onClick="snumero"
        android:id="@+id/snumero"
        android:layout_below="@id/puntuacion"
        android:layout_margin="5dp"
        android:background="#1D5F79"
        />
    <Button
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:text="ENVIAR NÚMERO"
        android:textColor="#FFF"
        android:fontFamily="monospace"
        android:textSize="20dp"
        android:onClick="enumero"
        android:id="@+id/enumero"
        android:layout_below="@id/snumero"
        android:layout_margin="5dp"
        android:background="#1D5F79"
        />
    <Button
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:text="RESULTADOS"
        android:textColor="#FFF"
        android:fontFamily="monospace"
        android:textSize="20dp"
        android:onClick="resultados"
        android:id="@+id/resultados"
        android:layout_below="@id/enumero"
        android:layout_margin="5dp"
        android:background="#1D5F79"
        />
    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/prrrr"/>
</RelativeLayout>